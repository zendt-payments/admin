# Copy to keystore.properties (next to this file), fill in real values, never commit secrets.
#
# Paths for storeFile are relative to the android/ directory, e.g. zendt-release.jks
#
# storeFile=zendt-release.jks
# storePassword=your-store-password
# keyAlias=zendt
# keyPassword=your-key-password
